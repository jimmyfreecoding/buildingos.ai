apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: buildingos-prod-grafana-data
  name: buildingos-prod-grafana-data
  namespace: buildingos-prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
