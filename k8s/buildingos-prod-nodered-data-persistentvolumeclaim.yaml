apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: buildingos-prod-nodered-data
  name: buildingos-prod-nodered-data
  namespace: buildingos-prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
