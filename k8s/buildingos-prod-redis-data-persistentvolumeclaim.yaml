apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: buildingos-prod-redis-data
  name: buildingos-prod-redis-data
  namespace: buildingos-prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
