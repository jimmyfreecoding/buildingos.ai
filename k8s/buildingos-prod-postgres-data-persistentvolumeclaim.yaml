apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: buildingos-prod-postgres-data
  name: buildingos-prod-postgres-data
  namespace: buildingos-prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
